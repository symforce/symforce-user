{#{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
    {% if sf.versionsession is not empty %}
        {% set targetUrl = sf.versionsession.get('_security.' ~ sf.versionsecurity.token.providerKey ~ '.target_path') %}
        {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>{% endif %}
    {% endif %}
{% endblock fos_user_content %}
        
        #}
        
{% extends "AppWebBundle::page_layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block sf_content_main %}
<style>
    
    
    .content {
        background: none repeat scroll 0 0 #EEEEEE;
        border-radius: 10px;
        border-top: 1px solid #DADADA;
        margin: 150px auto;
        padding-top: 10px;
        width: 782px;
    }
    .content-head {
        font-size: 1px;
        height: 8px;
        width: 782px;
    }
    .img {
        background: url("http://st2.dbank.com/netdisk/images/newdisk/resetpassword-btn.png?v=343309") no-repeat #fff;
    }
    .content-body {
        background: url("http://st2.dbank.com/netdisk/images/newdisk/resetpassword-body.png?v=343309") repeat-y;
        padding: 30px 0 50px;
        width: 782px;
    }
    .form {
        height: 70px;
        margin: 40px 0 0 0;
        position: relative;
        z-index:100;
    }

    .form .line7 {
        font-size: 16px;
        width: 50%;
        margin: 0 auto;
    }

    .content-foot {        
        background-position: -1px -12px;
        height: 38px;
        width: 782px;
    }
    .ico-gou {
        background: url("/bundles/appweb/images/imgs.gif") no-repeat -108px 0px;
    }
    .ico-46 {
        height: 46px;
        width: 46px;
    }
</style>

<div class="content">
    {#<div class="btn content-head"></div>#}
    <div class="content-body">
        <div class="form">
            <div id="resgSuccess_box" style="display: block;">

                <div class="line7">
                    <i class="col-app-4 ico-46 ico-gou "></i>
                    <div class="col-app-19" style=" padding: 10px 0">
                            <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
                            {% if sf.versionsession is not empty %}
                                {% set targetUrl = sf.versionsession.get('_security.' ~ sf.versionsecurity.token.providerKey ~ '.target_path') %}
                                {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>{% endif %}
                            {% endif %}  
                    </div>
                </div>



                {#<center class="line7">
                    <span class="img set-success"></span>

                    <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
                    {% if sf.versionsession is not empty %}
                        {% set targetUrl = sf.versionsession.get('_security.' ~ sf.versionsecurity.token.providerKey ~ '.target_path') %}
                        {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>{% endif %}
                    {% endif %}

                </center>#}
            </div>
        </div>
    </div>
    <div class="img content-foot"></div>
</div>
{% endblock %}

